namespace SX.Math.Enums
{
    public enum LexemOperatorType
    {
        Plus, Minus, Multiply, Divide,
        LessEq, MoreEq, Less, More, Equal, NotEqual,
        Or, And,
        Assign, Pointer, Property
    };

}
